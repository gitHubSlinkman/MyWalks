---
title: "test__plot_tempature"
author: "by Craig Slinkman"
date: "10/7/2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## Load packages

```{r load_packages}
require( tidyverse )                    # I live in the tidyverse ...
require( lubridate )                    # For consistent date processing ...
require( cowplot )                      # For theme_cowplot() ...
```
## Load functiions

```{r load_functions}
source('D:/R-Projects/MyWalks/functions/get_day_routes_v01.R')
source('D:/R-Projects/MyWalks/functions/filter_by_days_v01.R')
source('D:/R-Projects/MyWalks/functions/plot_ts_v01.R')
```

## Get day_routes data

```{r get+day_routes}
all_day_routes <- get_day_routes()

dim( all_day_routes )
```


## Create test data sets


## Create 365 day data set

Note that as of 12/1/202 I still don'r have 365 days of data.  So this first data set will be "short" until I do.  Also I am going back and adding weather data as I add new days to the data set.  

### Create last_365_days tibble

```{r}
last_365_days <- filter_by_days( all_day_routes, 365 )

dim( last_365_days )
```

### Last 180 days tibble

```{r}
last_180_days <- filter_by_days( all_day_routes, 180 )

dim( last_180_days )
```


### Last_090_days

```{r}
last_090_days <- filter_by_days( all_day_routes, 90 )

dim( last_090_days )

```

### Last 30 days tibble

```{r}
last_030_days <- filter_by_days( all_day_routes, 30 )

dim( last_030_days )
```


### Last_007_day_routes

```{r}
last_007_days <- filter_by_days( all_day_routes, 7)

dim( last_007_days )
```


### 365 day test of time series plot 

```{r}

plot_ts( data = last_365_days,
         variable = "temp",
         label    = "Temperature(Farenheit)",
         title    = "365 day plot of Temperature" )

plot_ts( data = last_365_days,
         variable = "dp",
         label = "Dew point",
         title = "365 day plot of dew point" )


```

### 180 day test plot oftime series plot

```{r}
plot_ts( data =last_180_days,
         variable = "temp",
         label    = "Temperature(Farenheit)",
         title    = "180 day time series plot of Temperature" )

plot_ts( data =last_180_days,
         variable = "dp",
         label    = "Temperature(Farenheit)",
         title    = "180 day time series plot of dew point" )

```

### 90 day test of time series plots

```{r}
plot_ts( data =last_090_days,
         variable = "temp",
         label    = "Temperature(Farenheit)",
         title    = "90 day time series plot of Temperature" )

plot_ts( data =last_090_days,
         variable = "dp",
         label    = "Temperature(Farenheit)",
         title    = "90 day time series plot of dew point" )

```


### 30 day test of time series plot

```{r}
plot_ts( data =last_030_days,
         variable = "temp",
         label    = "Temperature(Farenheit)",
         title    = "30 day time series plot of Temperature" )
```

### 7 day day test of time series plot

```{r}
plot_ts( data =last_007_days,
         variable = "temp",
         label    = "Temperature(Farenheit)",
         title    = "7 day time series plot of Temperature" )
```

